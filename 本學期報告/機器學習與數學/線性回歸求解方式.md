# Linear Regression 全解法完整教材（18 種）

本教材整理「線性回歸」的所有主流求解方式，包含：

- 解析解（Closed-form）
- 數值線性代數解法（SVD / QR / Cholesky）
- 迭代式求解（GD / SGD / CG / LBFGS）
- 正則化回歸（L2 / L1 / Elastic Net）
- Robust 回歸（Huber / RANSAC）
- 統計降維回歸（PCA Regression / PLS）

並含 **公式、用途、數值穩定性、Python 程式碼示例**。

---

# 目錄

1. 問題定義  
2. 解析解法（Closed-form）  
3. 數值分解法（SVD / QR / Cholesky）  
4. 大規模資料的迭代法（GD / SGD / Mini-batch / CG）  
5. 正則化線性回歸（Ridge / Lasso / Elastic Net）  
6. 統計/降維方法（PCA Regression / PLS）  
7. Robust 回歸（Huber / RANSAC）  
8. 機器學習式優化（Newton / LBFGS）  
9. Python 程式匯總  
10. 總比較表  

---

# 1️⃣ 問題定義（Linear Regression）

給定：

$$
y = X\beta + \epsilon
$$

最小二乘解（Least Squares）找：

$$
\beta^\* = \arg\min_\beta \|X\beta - y\|_2^2
$$

---

# 2️⃣ 解析解法（Closed-form Solutions）

## 2.1 Normal Equation

$$
\beta = (X^TX)^{-1}X^Ty
$$

⚠ 缺點：  
- 必須 invert \(X^TX\)
- multicollinearity 時會數值爆炸  
- 不適合高維或大型資料

---

## 2.2 Moore-Penrose Pseudo-inverse

$$
\beta = X^{+} y
$$

`X⁺ = V Σ⁺ Uᵀ`（SVD 計算）  
⚡ 最穩定的 analytical 解。

---

## 2.3 SVD 解（最穩定）

$$
X = U\Sigma V^T
$$

$$
\beta^\* = V\Sigma^{+}U^Ty
$$

---

# 3️⃣ 數值線性代數解法（適合中型資料）

## 3.1 QR Decomposition

$$
X = QR
$$

$$
\beta = R^{-1}Q^Ty
$$

🎯 實務常用，比 Normal Equation 穩定。

---

## 3.2 Cholesky Decomposition（僅 SPD）

$$
X^TX = LL^T
$$

$$
LL^T\beta = X^Ty
$$

⚠ 若 \(X\) 有共線性 → 無法使用。

---

## 3.3 LU Decomposition（間接方式）

解 normal equation：

$$
(X^TX)\beta = X^Ty
$$

不如 QR / SVD 穩定。

---

# 4️⃣ 大規模資料（Iterative Methods）

## 4.1 Gradient Descent（GD）

更新式：

$$
\beta := \beta - \alpha X^T(X\beta - y)
$$

---

## 4.2 Stochastic Gradient Descent（SGD）

每次更新一筆：

$$
\beta := \beta - \alpha (x_i^T\beta - y_i)x_i
$$

🎯 適合深度學習、大型資料。

---

## 4.3 Mini-Batch Gradient Descent

比 GD 快，比 SGD 更穩。

---

## 4.4 Conjugate Gradient（CG）

解：

$$
(X^TX)\beta = X^Ty
$$

非常適合：「大型、稀疏」資料。

---

# 5️⃣ 正則化 Linear Regression

## 5.1 Ridge Regression（L2 正則化）

$$
\beta = (X^TX + \lambda I)^{-1}X^Ty
$$

💡 可用：
- SVD
- QR
- Cholesky  
求解。

---

## 5.2 Lasso（L1）

$$
\beta = \arg\min_\beta \|X\beta - y\|^2 + \lambda \|\beta\|_1
$$

無閉式解 → Coordinate Descent。

---

## 5.3 Elastic Net（L1 + L2）

$$
\|X\beta - y\|^2 + \lambda_1\|\beta\|_1 + \lambda_2\|\beta\|_2^2
$$

---

# 6️⃣ 統計降維法 Regression

## 6.1 PCA Regression

1. 對 X 做 PCA  
2. 取前 k PCs  
3. 在低維空間回歸

可解決 multicollinearity。

---

## 6.2 PLS（Partial Least Squares）

同時考慮：
- X 的變異
- 與 y 的相關性

常用於化學光譜、生醫資料。

---

# 7️⃣ Robust Regression

## 7.1 Huber Regression

Loss：

$$
L_\delta(r)=
\begin{cases}
\frac{1}{2}r^2, & |r|\le\delta\\
\delta(|r| - \frac{1}{2}\delta), & |r|>\delta
\end{cases}
$$

適合含 outlier。

---

## 7.2 RANSAC Regression

隨機抽子集 → 估計模型  
適合雜訊非常高的資料。

---

# 8️⃣ 機器學習優化（ML Optimization Methods）

## 8.1 Newton’s Method

$$
\beta_{k+1} = \beta_k - H^{-1}\nabla J
$$

---

## 8.2 Quasi-Newton（L-BFGS）

深度學習常用  
適合中大型資料。

---

