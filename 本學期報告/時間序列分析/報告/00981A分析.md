## 00981A
- 資料下載
- 資料視覺化
- 使用迴歸分析的計劃，以預測股價並評估技術指標的影響。
  - 準備迴歸分析數據: 選擇 'Close' 作為目標變數，'SMA_20'、'SMA_60' 和 'RSI' 作為特徵變數。處理這些變數中的任何缺失值，確保數據集適用於迴歸模型。
  - 劃分訓練集和測試集: 將準備好的數據集劃分為訓練集和測試集，以便訓練模型並評估其在未見數據上的性能。
  - 訓練線性迴歸模型: 使用訓練數據訓練一個線性迴歸模型，以建立股價與技術指標之間的關係。
  - 評估模型性能: 使用測試數據評估訓練好的線性迴歸模型。計算 R-squared、均方誤差 (MSE) 和均方根誤差 (RMSE) 等指標來衡量模型的預測準確性。
  - 視覺化迴歸結果: 繪製模型的預測值與實際收盤價之間的對比圖，並可選地繪製殘差圖，以視覺化評估模型的擬合程度和誤差分佈。圖表中應包含適當的標題和圖例。
  - 總結迴歸分析結果: 總結迴歸分析的結果，包括模型解釋了多少股價變動 (R-squared)，以及每個技術指標對股價的影響程度 (係數)。
  - Final Task: 提供完整的迴歸分析報告和任何進一步的見解。

### 資料下載
```python
# 這是概念程式碼，無法保證 t03g 存在於 FinMind 中
from FinMind.data import get_data

stock_id = 't03g' 

try:
    df = get_data(
        dataset='TaiwanStockPrice', # 請替換成正確的資料集名稱
        stock_id=stock_id,
        start_date='2024-01-01' 
    )
    if df.empty:
        print(f"❌ 無法在 'TaiwanStockPrice' 資料集中找到 {stock_id} 的資料。")
    else:
        print(df.head())
except Exception as e:
    print(f"🚨 嘗試下載 {stock_id} 時發生錯誤。")

```
### 資料視覺化
- 產生 Python 程式碼，使用 matplotlib 繪製先前模擬的 00981A 股價 (Close) 與 20 日/60 日移動平均線 (SMA_20, SMA_60) 的趨勢圖
